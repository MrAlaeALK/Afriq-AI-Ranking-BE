# ============================================
# DEVELOPMENT PROFILE
# ============================================
# Activated with: -Dspring.profiles.active=dev or SPRING_PROFILES_ACTIVE=dev

# Show SQL queries in console (useful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Use update mode for development (auto-create/update tables)
spring.jpa.hibernate.ddl-auto=update

# Development CORS - allow localhost
# (Override in SecurityConfig if needed)

# File upload directory - local for development
file.uploaddir=${FILE_UPLOAD_DIR:C:/Users/Lenovo/Downloads/Afriq-AI-Upload}

# FastAPI - local development
fastapi.base-url=${FASTAPI_BASE_URL:http://localhost:8000}

# Frontend - local development
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Logging
logging.level.com.pfa.pfaproject=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

